import React from 'react'

const NavBar = () => {
    return ( 
        <nav className="Menu">
            <a href="#">Home</a>
            <a href="#">Search</a>
            <a href="#">Playlist</a>
        </nav>
     );
}
 
export default NavBar;